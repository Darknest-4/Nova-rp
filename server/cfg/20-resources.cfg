# ─────────────────────────────────────────────────────────────────────────────
#  20-resources.cfg — indítandó resource-ok
#
#  Phase 2: kézzel karbantartott.
#  Phase 6-tól: a `nova resources:generate` parancs állítja elő a feature
#  flagekből, és a CI ellenőrzi, hogy a fájl naprakész-e. Egy kikapcsolt
#  feature resource-a onnantól be sem kerül ide — el sem indul.
#
#  A sorrend KÖTÖTT: a függőségeknek előbb kell indulniuk.
# ─────────────────────────────────────────────────────────────────────────────

# ── Cfx alap-resource-ok (cfx-server-data) ───────────────────────────────────
ensure mapmanager
ensure chat
ensure spawnmanager
ensure sessionmanager
ensure basic-gamemode
ensure hardcap

# ── Third-party függőségek (vendor/, pinelt verzió) ──────────────────────────
# A `nova vendor:install` telepíti őket. Licenc és verzió: docs/dependencies/
# ensure oxmysql
# ensure ox_lib

# ── NOVA mag ─────────────────────────────────────────────────────────────────
ensure nova_lib
ensure nova_bootstrap
